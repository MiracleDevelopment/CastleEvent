<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottom_sheet"
    android:layout_width="match_parent"
    android:layout_height="270dp"
    android:background="@color/colorBackgroundBottomSheet"
    android:orientation="vertical"
    app:behavior_hideable="false"
    app:behavior_peekHeight="48dp"
    app:layout_behavior="android.support.design.widget.BottomSheetBehavior">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorBarTickets"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_Start_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="2dp"
            android:fontFamily="@font/circular"
            android:text="@string/tv_price_start"
            android:textColor="@color/colorTextGetTickets"
            android:textSize="16sp" />

        <TextView
            android:id="@+id/tv_get_tickets"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="2dp"
            android:text="@string/tv_get_tickets"
            android:textColor="@color/colorTextGetTickets"
            android:textSize="18sp"
            android:textStyle="bold" />
    </LinearLayout>

    <include layout="@layout/layout_bottom_sheet" />
</LinearLayout>